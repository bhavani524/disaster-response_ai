{
  "name": "rivermont-decision-intelligence-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python -m http.server 8080 --bind 127.0.0.1",
    "start": "python -m http.server $PORT --bind 0.0.0.0"
  }
}
