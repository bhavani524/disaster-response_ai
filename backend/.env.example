# Optional — set ONE of these to have the /api/chat assistant phrase answers
# with a real LLM instead of the built-in offline template engine.
# The app works fully without any of these (offline mode).

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

PORT=4001
